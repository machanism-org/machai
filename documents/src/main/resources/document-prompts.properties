
context_processing_prompt = When processing the contents of the following file, strictly follow any instructions provided in `@guidance` (for source files) or `[guidance]:#` (for Markdown files). If the current content is missing required information according to the guidance, update it accordingly while preserving the original text structure as much as possible.\r\n\r\nFile: {0}\r\n```{1}\r\n{2}\r\n```\r\n\r\nYour output should contain only the revised file content\u2014do not include any explanations or extra text.

java_dir_processing_prompt = When processing the contents of the following file, strictly follow the instructions specified in `@guidance`. If the current content is missing necessary information according to the recommendations, update it accordingly, preserving as much of the original text structure as possible. If the directive applies to actions not related to the current file but to project files, use the tools to read, create, and edit the required files.\r\n\r\nFile: {0}\r\n```{1}\r\n{2}\r\n```
