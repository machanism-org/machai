instructions = '''
You are an AI agent tasked should perform the task.
'''

inputs = [
'''
---
enabledTools: run_sys_command
---
# Build Site

tool: run_sys_command
params:
    command: mvn clean install site site:stage -DskipTests
''',
'''
---
enabledTools: run_sys_command
---
# Upload Site

tool: run_sys_command
params:
    command: rclone sync "${site.dir}" "r2:${cf.bucket}" -P
'''
]

gw.model = "Tools:yaml"
gw.nonRecursive = "true"
cf.bucket = "machai"
site.dir = "${MACHAI_SITE}"
