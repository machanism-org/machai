@startuml
!theme black-knight
title Semantic Search and Report Generation for Artifacts

start
:User inputs application assembly\nrequirements query;
:Parse query and generate query embedding;
:Perform semantic search based on embedding;

if (Matching bindex objects found?) then (Yes)
  :Return list of matching bindex objects;
  :Generate bindex report and find\nrelevant artifacts;
  :Return report to the User;
  stop
else (No)
  :No matching bindex objects found;
  :Return "No results found" to the User;
  stop
endif
@enduml