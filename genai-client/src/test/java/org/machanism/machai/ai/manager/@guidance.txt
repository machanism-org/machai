- **Review and update all existing unit tests in this folder to maximize coverage and quality for the package `org.machanism.machai.ai.manager` only.**  
  *Do not include or process any sub-packages.*
- **Create new, high-quality unit tests as needed to ensure that every public and package-private class and method in the required source folder is fully tested.**
- **Ensure that the overall test coverage for the required source folder is at least 90%.**
- **Use the Maven JaCoCo plugin to measure test coverage:**
  - Run the Maven JaCoCo plugin to generate a coverage report (typically at `target/site/jacoco/jacoco.xml`).
  - Read and analyze the JaCoCo report to identify uncovered or under-covered classes and methods.
  - Focus test creation and updates on areas highlighted by the JaCoCo report as lacking sufficient coverage.
- Achieve comprehensive coverage of all core logic, edge cases, and error handling scenarios.
- Refactor or enhance existing tests to improve clarity, maintainability, and coverage where necessary.
- If necessary to achieve coverage, refactor production code for improved testability, but do not change its functionality.
- Use descriptive test method names and meaningful assertions.
- Follow the Arrange-Act-Assert (AAA) structure in each test.
- Mock dependencies
 where appropriate to isolate the units under test.
- Place all test files in this directory, mirroring the package structure of the source files.
- **Do not remove any existing tests unless they are redundant or incorrect; instead, update or supplement them as needed.**