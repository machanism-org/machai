<Recipe name="Submit Files">
	<About>
		<description>
This recipe automates the process of sending GenAI requests using input files generated by the "ghostwriter" program. 
It scans the specified project directory for matching input files, loads their contents, and submits each one as a request to the GenAI service. 
This recipe is specifically designed to process files generated by the [NoneProvider](http://machai.machanism.org/genai-client/index.html) class.
    </description> 
	</About> 
	<Var name="Project_Dir" init="default" type="path" source="mavenBaseDir" /> 
	<!-- Inputs processing --> 
	<Listdir name="Inputs Files" path="$var{Project_Dir}" filter="^(?!instructions\.txt$).*\.txt$" dirFilter=".*\.machai\\docs-inputs.*" /> 
	<Out name="Inputs Files" /> 
	<Loop name="inputsFile" source="Inputs Files">
		<Out name="inputsFile" type="path" level="warn" /> 
		<Load name="inputs" file="$var{inputsFile}" /> 
		<Var name="inputs" init="console" type="text" /> 
		<Task name="Submit Prompt" level="debug" /> 
	</Loop> 
</Recipe> 
