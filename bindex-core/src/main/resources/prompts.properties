# Prompt templates used by org.machanism.machai.bindex.Picker
#
# Keep this file on the classpath so Picker can initialize in unit tests.
#
# Parameters:
#  {0} - JSON schema for the Classification object
#  {1} - user query
classification_instruction=You are a classifier. Using the following JSON schema: {0} classify the user query: {1}. Output ONLY valid JSON (no markdown), as an array of Classification objects.
