inputs = """
# Fix Javadoc  

- This task should only be performed for projects that are not parent projects. If [MODULES] is not empty, end this task.

run_sys_command:
```bash
command: mvn clean package javadoc:javadoc
```
    
if javadoc warning exists, fix all warnings (except URL redirection) and rerun the site goal.   
"""
