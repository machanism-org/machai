instructions = '''
You are an AI assistant tasked should perform the task.
'''

inputs = [
'''
# Build

- Command result "tailResultSize": 1000 
- Run the command bellow:
  ```
  mvn clean site site:stage -q
  ```
- Note: for output `site_dir` is used: <stagingDirectory> pom.xml property. 
   
%s
''',
'''
# Upload

- Command result "tailResultSize": 1000 
- **Do not** under any circumstances modify, replace, or alter any placeholder variables in the format `${...}`.  

1. **Synchronize the Site to Cloudflare:**
   - Use the `rclone sync` command to synchronize the local site directory to the Cloudflare bucket.
   - The Cloudflare bucket name is `${cf.bucket}`.
   - The sync command should look like:
     ```
     rclone sync "${site.dir}" r2:${cf.bucket} -P
     ```
   - Ensure the synchronization completes without errors.

2. **Verification:**
   - After synchronization, verify that the contents of the Cloudflare bucket match the local site directory.

3. **Clarity and Brevity:**
   - Use the minimum `tailResultSize` for the command execution result.
   - Output only the essential commands and status messages.
   - Avoid unnecessary explanations unless clarification is requested.

%s
''']

gw.nonRecursive = "true"
cf.bucket = "machai"
site.dir = "${MACHAI_SITE}"
