instructions = '''
You are an AI agent tasked should perform the task.
'''

inputs = [
'''
---
enabledTools: run-sys-command
---
# Build Site

tool: run-sys-command
params:
    command: mvn clean install site site:stage
''',
'''
---
enabledTools: run-sys-command
---
# Upload Site

tool: run-sys-command
params:
    command: rclone sync "${site.dir}" "r2:${cf.bucket}" -P
'''
]

gw.model = "Tools:yaml"
gw.nonRecursive = "true"
cf.bucket = "machai"
site.dir = "${MACHAI_SITE}"
