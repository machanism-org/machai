instructions = '''
You are an AI assistant tasked should perform the task.
'''

inputs = [
'''
# Build Site

tool: run_sys_command
params:
    command: mvn clean package site site:stage
''',
'''
# Upload Site

tool: run_sys_command
params:
    command: rclone sync "${site.dir}" "r2:${cf.bucket}" -P
'''
]

gw.model = "Tools:yaml"
gw.nonRecursive = "true"
cf.bucket = "machai"
site.dir = "${MACHAI_SITE}"
