inputs = '''
# Context

- Use `cmd /c` for Windows (e.g., `cmd /c your-command`) and `sh -c` for Unix/Linux (e.g., `sh -c 'your-command'`) to ensure proper shell execution.
- Do not change variable plaiceholder `${...}`, it will be change by the application.
- The main source and resource files are located in: `[Main Source & Resource Folders]`.
- Determine the correct value for [COMPONENT_KEYS] based on the `Project Layout` parameters:
  - If [Child Projects]: `not defined`: set [COMPONENT_KEYS] to `[Project Identifier]`
    else set [COMPONENT_KEYS] to `[Project Identifier]`.
- Construct the `SONARQUBE_URL` as follows: 
`http://${sonar.token}:@${sonar.url}/api/issues/search?issueStatuses=OPEN&impactSeverities=${severities}&componentKeys=org.machanism.machai:[COMPONENT_KEYS]&ps=30`
- Use this URL to retrieve SonarQube issues for the relevant project component.
- When processing source code, the path from the Sonar issue report to the source file should be used starting with `src/`; characters before it should be removed.
- If remaining (still OPEN) issues requiring further iteration: continue with the next iteration and address these remaining items one-by-one (including fetching next pages if possible or needed). 

# Additional Instruction

- Ensure all fixes are applied to the correct source files and are properly tested.
— Re-run the Sonar plugin command: `mvn clean install org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.token=${sonar.token} -Dsonar.host.url=http://${sonar.url}`.
- If After re-running the API query, Sonar still reports issues. the next iteration should fetch the next pages (or increase `ps`) and continue fixing issue-by-issue.
— Run the Sonar plugin command before get the report.

%s

— Re-run the Sonar plugin command to check the result of the fix.
'''

gw.scanDir="glob:."
severities="LOW,MEDIUM,HIGH,BLOCKER"
sonar.url="192.168.0.177:9000"
sonar.token="sqa_1ca2fc4cb0449e8a70c08933bb8a82cba89d91df"

